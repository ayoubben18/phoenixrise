import {CanvasRevealEffectCard} from "@/components/CanvasRevealEffectCard";
import { FaReact } from "react-icons/fa";
import {blue} from "next/dist/lib/picocolors";

export default function page() {
    return (
        <div className='section-style'>
            Under developement
            {/*<CanvasRevealEffectCard icon={<FaReact size={80}/>} title={'React'}/>*/}
        </div>
    );
}